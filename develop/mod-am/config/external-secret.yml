---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: secret-global
  namespace: mod-am-dev
spec:
  refreshInterval: "30s"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: secret-global
  data:
    - secretKey: private-key
      remoteRef:
        key: secret-global
        property: private-key
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: test-secret-secret
  namespace: mod-am-dev
spec:
  refreshInterval: "30s"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: test-secret-secret
  dataFrom:
    - extract:
        key: develop/mod-am/test-secret

